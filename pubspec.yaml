name: vsc_tasks_mcp
version: 1.0.0+1
description: MCP server that exposes VSCode tasks.json definitions as executable tools for AI agents
repository: https://github.com/eaglesakura/vsc_tasks_mcp/

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  dart_mcp: ^0.3.3
  path: ^1.9.1
  stream_channel: ^2.1.4
  meta: ^1.16.0
  json_annotation: ^4.9.0
  freezed_annotation: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  freezed: ^3.2.3
  build_runner: ^2.5.2
  json_serializable: ^6.10.0
  test: ^1.25.0

executables:
  vsc_tasks_mcp:
